# 지인 테스트용 실행 가이드

## 1. 현재 상태

이 프로젝트는 로컬에서 이미 정상 동작하는 상태를 확인했습니다.

- 백엔드 헬스 체크: `http://127.0.0.1:8000/health` → `{"status":"ok"}`
- 프론트 엔드 로딩: `http://127.0.0.1:4173` → HTML 응답 확인
- 프론트 프로덕션 빌드: `npm run build` → 성공

즉, 앱 자체는 작동 중입니다.

## 2. 왜 공개 URL이 불안정한가

현재 이 환경에서는 로컬 PC를 외부에 안정적으로 공개하는 `localtunnel`/`serveo` 같은 무료 터널이 종종 네트워크/보안 정책에 막혀서 실제 공개 테스트가 어렵습니다.

그래서 지인 테스트용으로는 다음 중 하나가 필요합니다.

1. VPS/서버 1대 준비
2. Docker 또는 Nginx로 배포
3. HTTPS 및 실제 도메인/서브도메인 연결
4. 지인에게 공개 URL 공유

## 3. 가장 현실적인 옵션

### 옵션 A: VPS 배포 (권장)

- 서버는 Ubuntu 22.04 이상
- 최소 사양: 2GB RAM, 20GB SSD
- Git pull → Docker Compose 실행
- 외부 URL 공개

### 옵션 B: 로컬 네트워크 테스트

- 지인들이 같은 Wi-Fi/같은 내부망에 있어야 가능
- 하지만 외부 친구들이 직접 접근하는 것은 불가

## 4. VPS에서의 배포 절차

```bash
git clone <repo-url>
cd <repo>
cp .env.example .env
# 실제 키를 .env에 채움

docker compose up --build -d
```

배포 후 확인:

```bash
curl http://localhost:8000/health
curl http://localhost:5173
```

## 5. 공개 URL 세팅

배포 서버에서 다음 중 하나를 선택합니다.

- Cloudflare Tunnel
- Nginx + Let's Encrypt
- 도메인 연결 후 HTTPS

가장 안정적인 구성은 `Cloudflare + 도메인 + HTTPS` 입니다.

## 6. 지인 테스트 전 체크리스트

- `/health` 정상 동작
- 메인 페이지 로딩 정상
- 주소 입력 폼 동작
- OCR 업로드 흐름 확인
- 결과 페이지 확인
- 모바일 환경 확인
- 개인정보/동의 문구 확인
- 비정상 입력 시 오류 메시지 확인

## 7. 피드백 수집 포인트

지인들에게 아래를 각각 체크해달라고 요청하면 좋습니다.

- 입력 단계가 직관적인가
- 보증금/주소 입력이 편한가
- 위험도 결과가 이해하기 쉬운가
- 약관과 동의 문구가 충분히 읽히는가
- OCR 업로드 과정이 불편한가
- 결과 설명이 신뢰감 있게 느껴지는가
- 이 앱을 실제로 쓸 만한가

## 8. 결론

현재 이 환경에서 무료 터널만으로는 안정적인 지인 테스트를 모두 보장하기 어렵습니다.

지인 사용 피드백을 받기 위해서는 실제 서버 배포가 필요합니다.

이 프로젝트는 이미 로컬 검증이 끝났고, 배포 환경만 맞추면 친구 테스트는 곧바로 진행할 수 있습니다.
